
    <style>
        /* Algemene styling overgenomen van main.ejs of index.ejs */
        body {
            font-family: 'Montserrat', Arial, sans-serif;
            background: linear-gradient(120deg, #6a11cb 0%, #2575fc 100%);
            color: #fff;
            margin: 0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center; /* Centraal de content verticaal */
            align-items: center; /* Centraal de content horizontaal */
            text-align: center; /* Centraal de tekst */
        }
        header {
            padding: 2rem 0 1rem 0;
        }
        h1 {
            font-size: 3.5rem; /* Grotere titel */
            margin-bottom: 1rem;
            letter-spacing: 2px;
            text-shadow: 2px 2px 5px rgba(0,0,0,0.3);
        }
        p {
            font-size: 1.4rem;
            margin-bottom: 2.5rem;
            max-width: 700px; /* Beperk breedte voor leesbaarheid */
            margin-left: auto;
            margin-right: auto;
        }
        .main-buttons {
            display: flex;
            flex-direction: column; /* Knoppen onder elkaar */
            gap: 20px; /* Ruimte tussen de knoppen */
            margin-top: 3rem;
        }
        .cta-btn {
            background: #fff;
            color: #2575fc;
            padding: 1.2rem 3rem;
            border: none;
            border-radius: 40px; /* Rondere knoppen */
            font-size: 1.3rem;
            font-weight: bold;
            cursor: pointer;
            box-shadow: 0 6px 20px rgba(0,0,0,0.2);
            transition: background 0.3s ease, color 0.3s ease, transform 0.2s ease;
            text-decoration: none;
            display: inline-block;
            min-width: 250px; /* Minimum breedte voor consistentie */
        }
        .cta-btn:hover {
            background: #2575fc;
            color: #fff;
            transform: translateY(-3px); /* Licht zweef-effect */
        }
        footer {
            margin-top: auto; /* Dwingt de footer naar de onderkant */
            padding: 1.5rem 0;
            font-size: 1rem;
            opacity: 0.9;
        }
    </style>

        <h1><%= title %></h1>
        <p><%= message %></p>

        <div class="main-buttons">
            <a href="/menu" class="cta-btn">Bekijk ons Menu</a>
            <a href="/admin" class="cta-btn">Admin Dashboard</a>
        </div>

        &copy; <%= new Date().getFullYear() %> Dev2 Eindopdracht. Gemaakt door jouw naam.
 